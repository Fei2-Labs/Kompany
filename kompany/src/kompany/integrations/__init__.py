"""Built-in integrations shipped with Kompany Core.

These are concrete :class:`kompany.plugins.contract.Integration` /
``Tool`` implementations so agents can perform real external actions
(send email, ...) instead of only producing drafts. Pro / community
packages add more via the ``kompany.integrations`` entry-point group.
"""
